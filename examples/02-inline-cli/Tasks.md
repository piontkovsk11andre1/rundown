# Inline CLI Tasks

These tasks use the `cli:` prefix to run shell commands directly, without an external worker.

- [x] cli: mkdir -p build
- [ ] cli: echo "Hello from md-todo!" > build/hello.txt
- [ ] cli: cat build/hello.txt
- [ ] cli: npm test

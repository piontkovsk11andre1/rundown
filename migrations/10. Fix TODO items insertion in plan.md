When we run `plan` we determine where to insert TODO items based on headers and other things. We don't need that. 
Please insert TODO items in the end of file (or insert inside the existing list). 

- [x] Update `plan` insertion logic to stop computing TODO placement from headers or other structural heuristics.
- [x] Implement fallback insertion that appends new TODO items at the end of the Markdown file.
- [x] Detect an existing TODO list and append new TODO items into that list instead of creating a separate placement.
- [x] Add/adjust tests to verify both behaviors: append to existing list and append at end of file when no list exists.

When we run `rundown test` it should run tests against the materialized source directory by default, and support prediction mode with `rundown test --future` (latest snapshot + migrations) and `rundown test --future <n>` (previous snapshot + migrations up to target).

The two modes must be mutually exclusive in prompt behavior: future mode reads only design + migrations, while materialized mode ignores design/specs/migrations and tests only the source directory.

- [x] Update `rundown test` mode selection so the default path runs tests against the materialized source directory.
- [x] Add `--future` handling so `rundown test --future` resolves the latest snapshot and applies migrations before testing.
- [x] Add numeric target handling so `rundown test --future <n>` resolves the previous snapshot and applies migrations up to the requested target before testing.
- [x] Enforce mutually exclusive prompt/context inputs so future mode reads only design + migrations and materialized mode ignores design/specs/migrations.
- [x] Add or update tests that cover default materialized mode, `--future`, and `--future <n>` behavior including mode-specific input isolation.

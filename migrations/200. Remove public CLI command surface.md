# Remove public CLI command surface

This migration reduces the public CLI to the commands used by the simplified rundown runtime. Removed commands should disappear from help, command registration, public app surface, tests, and user-facing docs. Keep `run --all` as the multi-task execution surface; remove the separate `all` command.

- [ ] Update `src/presentation/cli.ts` to remove public command registrations, imports, and mode constants for `agent`, `research`, `explore`, `translate`, `all`, `materialize`, `start`, `migrate`, `predict`, `snapshot`, `compact`, `test`, `revert`, and `workspace`, then add a `repair` command that opens the rundown repair agent.

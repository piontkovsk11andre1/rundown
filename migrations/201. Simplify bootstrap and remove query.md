# Simplify bootstrap and remove query

Research is no longer a public or bootstrap phase. `make` and `do` should behave like the previous `--skip-research`/`--raw` path unconditionally. `make` creates a seed Markdown file and runs `plan`; `do` does the same bootstrap and then executes all tasks in that file.

- [x] Remove `--skip-research` and `--raw` from the public `make` command, then make `make` always create the seed Markdown file and run only `plan`.
- [x] Make `do` use the same plan-only bootstrap as `make`, then execute all tasks in the generated Markdown file without any research phase.
- [x] Remove the public `query` command registration, command action wiring, app-surface method, help text, docs references, and tests.
- [x] Remove now-unused public research orchestration dependencies from retained command paths so no retained public command calls `researchTask`.

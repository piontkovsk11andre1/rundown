I'll read the prompt file and understand the task, then explore the codebase to research and enrich the source document.
Now I need to deeply explore the codebase to understand the plan/step execution system.
Good overview, but I need more specific details about the plan phase implementation. Let me dig deeper.
Good, but I still need the specific code details for the plan phase. Let me look at the actual source files.
Let me read the actual source files directly.
Now let me read the planner domain file, the defaults/templates, and the iteration/execution files.
Now let me look at the plan template and the CLI options, and how phases are orchestrated.
Now let me look at the run-task-execution.ts to understand the main execution loop and how tasks are iterated.
Now let me also check how the `make` and `do` commands orchestrate the pipeline, and look at migration #43 ("Plan never stops") for context on convergence-driven planning.
Now I have enough context. Let me also check what the `create-app.ts` looks like for the `planTask` interface and see how the defaults/shared-prefix template looks.
Now let me also look at some completed enriched documents to understand the expected output style.
I see the pattern - the document file itself gets overwritten with the enriched output. Let me also look at a clean enriched document to see the expected style better.
Now I have comprehensive understanding of the codebase. Let me write the enriched document.
The enriched document has been written to `migrations/44. Plan steps.md`. Here's a summary of the research and enrichments made:

**Research conducted:**
- Full plan scan loop implementation in `src/application/plan-task.ts` (1004 lines) — scan iteration, convergence detection, prompt building, validation, deep passes
- Plan validation in `src/domain/planner.ts` — `validatePlanEdit()` with `PlanEditStats { added, removed, reordered }`
- Plan template in `src/domain/defaults.ts` — `DEFAULT_PLAN_TEMPLATE` directive rules
- CLI option registration in `src/presentation/cli.ts` and `cli-options.ts` — `--scan-count` parsing
- Pipeline orchestration in `cli-command-actions.ts` — `make` (research+plan) and `do` (make+run-all) command flows
- Task execution loop in `run-task-execution.ts` and `run-task-iteration.ts` — how tasks are selected and executed
- Session management — each scan is already a clean standalone worker session; no session infrastructure changes needed
- Related migration #43 (Plan never stops) — convergence-driven unlimited planning mode

**Document enrichments:**
- **Feature specification** — clear description of `--max-items <n>` as an orthogonal constraint on planning output volume
- **Current behavior detail** — scan loop mechanics, validation stats, prompt structure, CLI parsing, all with file paths and line numbers
- **Design approach** — 8 sections covering enforcement point, partial scan handling (recommending accept-overshoot), prompt guidance, convergence outcome tracking, pipeline integration, deep planning interaction, session freshness
- **Interface changes** — `PlanTaskOptions`, `create-app.ts` changes
- **Edge cases** — max-items=0, convergence-before-limit, multi-file, interaction with `--scan-count` and migration #43
- **Affected files** — 6 files with specific change descriptions
- **Testing considerations** — unit, integration, and artifact/trace test categories
- **Acceptance criteria** — 7 concrete criteria

## Implementation tasks

### 1) CLI option and parsing

- [x] Add `--max-items <n>` option to the `plan` command in `src/presentation/cli.ts` (near line 243 alongside `--scan-count`)
- [x] Add `--max-items <n>` option to the `make` command in `src/presentation/cli.ts` (near line 283)
- [x] Add `--max-items <n>` option to the `do` command in `src/presentation/cli.ts` (near line 316)
- [x] Create `parseMaxItems` function in `src/presentation/cli-options.ts` (following the `parseScanCount` pattern at line 178) that returns `number | undefined`, accepting `undefined` to mean "no limit"; unlike `parseScanCount`, this must use `allowUndefined: true` in `parseIntOption` since there is no default value
- [x] Include a descriptive help string for the `--max-items` option on each command (e.g. `"Cap the total number of TODO items added across all scans (default: no limit)"`)
- [x] Wire `parseMaxItems` into `createPlanCommandAction` in `src/presentation/cli-command-actions.ts` (near line 428) and pass the value to `app.planTask()`
- [x] Wire `parseMaxItems` into `createMakeCommandAction` in `src/presentation/cli-command-actions.ts` (near line 530) and pass the value to `app.planTask()`
- [x] Wire `parseMaxItems` into `createDoCommandAction` in `src/presentation/cli-command-actions.ts` (near line 598) and ensure the value propagates correctly through the delegation chain to `make`

### 2) Interface and type changes

- [x] Add `maxItems?: number` field to `PlanTaskOptions` in `src/application/plan-task.ts` (line 99–115)
- [x] Add `maxItems?: number` field to `PlanTaskCommandOptions` in `src/create-app.ts` (line 115–131)
- [x] Update the `planTask` method signature on the `App` interface in `src/create-app.ts` (line 103) so the options type includes `maxItems`
- [x] Pass `maxItems` through from `PlanTaskCommandOptions` to the internal plan task use case in `src/create-app.ts` factory wiring (line 278–300)

### 3) Enforcement in the scan loop

- [x] Track cumulative inserted TODO count using the existing `insertedTotal` accumulator in `src/application/plan-task.ts` (line 546)
- [x] Add a post-scan check in the scan loop (after validation, near line 643) that compares `insertedTotal >= maxItems` and breaks the loop when the limit is reached
- [x] Emit an info-level message when the max-items cap terminates the scan loop (following the pattern of the convergence message at line 637)
- [x] Define a new `PlanConvergenceOutcome` value (e.g. `"max-items-reached"`) in `src/application/plan-task.ts` (near line 73) for when the item cap causes termination
- [x] Set `convergenceOutcome` to the new `"max-items-reached"` value when the loop terminates due to exceeding `maxItems`
- [x] Decide on accept-overshoot vs. reject-overshoot strategy: if the final scan adds items that push `insertedTotal` past `maxItems`, accept the overshoot (recommended) rather than rejecting the entire scan's edits

### 4) Prompt guidance for max-items

- [x] Pass `maxItems` and `insertedTotal` as template variables in `src/application/plan-task.ts` (near line 273–288) so the plan template can reference them
- [x] Update `buildPlanScanPrompt` in `src/application/plan-task.ts` (near line 875) to include remaining-item-budget guidance when `maxItems` is set (e.g. "You have budget for N more items")
- [x] Ensure prompt guidance is omitted entirely when `maxItems` is undefined (no-limit mode) to avoid confusing the worker
- [x] Consider passing remaining-item-budget to `buildPlanDeepPrompt` as well so deep-pass workers are aware of the constraint

### 5) Dry-run and print-prompt output

- [x] Update dry-run info output in `src/application/plan-task.ts` (near line 376–378) to display the `maxItems` value when set (e.g. `"Max items: 5"`)
- [x] Update print-prompt mode in `src/application/plan-task.ts` (near line 330–331) so that the rendered scan prompt reflects the budget guidance when `maxItems` is set (this is implicitly handled if `buildPlanScanPrompt` is updated, but verify the `printPrompt` path uses the same updated function signature)

### 6) Convergence metadata and output

- [x] Update `buildPlanConvergenceMetadata` in `src/application/plan-task.ts` (near line 846–858) to include `maxItems` and the new `"max-items-reached"` outcome in artifact metadata
- [x] Update run summary output to display when planning stopped due to `--max-items` limit being reached
- [x] Ensure the `"max-items-reached"` outcome is recorded distinctly from `"converged"` and `"scan-cap-reached"` in trace/artifact files

### 7) Interaction with deep passes

- [x] Verify that deep-pass item counts (accumulated in `insertedTotal` near lines 761–762) are included in the cumulative count checked against `maxItems`
- [x] Add the same `insertedTotal >= maxItems` break check after deep passes complete (near line 773) to prevent deep passes from generating items beyond the limit
- [x] Add an `insertedTotal >= maxItems` break check inside the inner deep-task loop (after line 762, within the `deepTaskIndex` iteration) to stop processing remaining parent tasks mid-pass when the budget is exhausted

### 8) Pipeline integration

- [x] Ensure `make` command in `src/presentation/cli-command-actions.ts` forwards `maxItems` from CLI options to `app.planTask()` (line 560)
- [x] Ensure `do` command in `src/presentation/cli-command-actions.ts` forwards `maxItems` through the `make` delegation chain without re-parsing or losing the value

### 9) Interaction with migration #43 (convergence-driven unlimited mode)

- [x] Ensure `maxItems` enforcement is compatible with unlimited scan mode from migration #43: the `maxItems` cap should trigger termination even when scan count is unlimited
- [x] When both `maxItems` and semantic convergence (`added === 0`) apply, the first one reached should terminate the loop and record the appropriate outcome
- [x] Ensure the emergency ceiling from migration #43 is still respected as a final fallback even when `maxItems` is set

### 10) Edge cases

- [x] Handle `maxItems` of 0: treat as "add no items" — skip planning entirely or terminate immediately after the first scan with zero additions allowed
- [x] Handle convergence-before-limit: when the planner converges naturally before reaching `maxItems`, record the convergence outcome (not `"max-items-reached"`)
- [x] Validate that `maxItems` is a positive integer (or 0) in `parseMaxItems`; reject negative values and non-integers with a clear error message

### 11) Testing

- [x] Add unit tests for `parseMaxItems` in the CLI options test suite: valid integers, zero, missing value (undefined), negative values, non-integer strings
- [x] Update the `createOptions` test helper in `__tests__/application/plan-task.test.ts` (line 1568) to support the optional `maxItems` field
- [x] Add unit tests verifying the scan loop terminates when `insertedTotal >= maxItems`
- [x] Add unit tests verifying `maxItems` overshoot behavior: a scan that pushes past the limit is accepted (not rejected)
- [x] Add integration tests verifying `--max-items <n>` works end-to-end with the `plan` command
- [x] Add integration tests verifying `--max-items <n>` propagates through `make` and `do` commands
- [x] Add tests verifying artifact/trace metadata includes the `"max-items-reached"` outcome when applicable
- [x] Add tests verifying that omitting `--max-items` does not change existing behavior (no limit enforced)
- [x] Add tests verifying interaction between `--max-items` and `--scan-count`: both constraints respected, first one reached terminates

### 12) Acceptance criteria validation

- [x] Verify: `rundown plan --max-items 5 <file>` stops after approximately 5 TODO items are added
- [x] Verify: `rundown plan <file>` without `--max-items` behaves identically to current behavior (no limit)
- [x] Verify: metadata/output clearly reports when `--max-items` caused termination vs. convergence vs. scan-cap
- [x] Verify: `--max-items` works correctly with both `make` and `do` pipeline commands
- [x] Verify: `--max-items` interacts correctly with `--scan-count` (both constraints active simultaneously)
- [x] Verify: deep passes respect the `--max-items` limit
- [x] Verify: existing `validatePlanEdit` safety rules remain enforced regardless of `--max-items` setting
- [x] Verify: `--dry-run` output includes the `--max-items` value when specified

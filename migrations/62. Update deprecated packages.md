When installing `rundown`, npm shows deprecation/outdated-package warnings (including `glob`-related warnings). We should identify which dependency chain triggers the warnings, update direct dependencies to supported versions, and confirm install output is clean or meaningfully improved.

Goal: reduce install-time deprecation noise and keep dependency tree current without breaking CLI behavior.

- [x] Reproduce current install warnings (`npm install`) and record which packages are flagged as deprecated/outdated.
- [x] Determine whether warnings come from direct dependencies or transitive dependencies and list the upgrade path.
- [x] Update relevant dependency versions in `package.json`/lockfile to supported equivalents.
- [x] Run tests/build to verify the updated dependency set does not break functionality.
- [x] Re-run install and confirm deprecation warnings are resolved or reduced; document any remaining transitive warnings with reason.
- [x] verify: build and test ok

Validation note (2026-04-11): re-ran `npm install` after dependency updates; install completes with no deprecation warnings (including no `glob`-related warnings). Remaining install output is limited to funding/audit notices, so there are no remaining transitive deprecation warnings to document.

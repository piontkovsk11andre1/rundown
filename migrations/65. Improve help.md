Rework the agent help template (`help.md`) so the assistant behaves as a capable `rundown` operator, not just a thin command lookup bot.

Current issue: prompt framing is too narrow ("ask a question, I return a command"). Desired behavior is an agent that understands intent and executes/coordinates work through `rundown` flows.

Target behavior examples:

- If user says "explore this" or "plan this", the agent should map directly to `rundown explore` / `rundown plan` workflows and proceed.
- Agent should understand `rundown start` workspace-link behavior and linked-source context.
- Agent should be aware of migrations/task-file structure conventions in this repo and use them correctly.
- Agent should remain generally helpful outside rundown-specific requests, but prefer rundown-native operations when applicable.

`rundown --help --everything` output (from migration 64) should be included as authoritative reference, but this migration is broader: improve the prompt instructions, behavior policy, and examples so the agent consistently does the right thing.

- [x] Rewrite `help.md` system instructions to emphasize intent-to-workflow mapping (not command-only Q&A).
- [x] Add explicit guidance/examples for `plan`, `explore`, `run`, `call`, and when to use each.
- [x] Add `rundown start` linked-workspace awareness guidance (workspace/source links and effective working context).
- [x] Add repository-specific guidance for migrations structure and how agent should operate on migration files.
- [x] Keep a fallback mode for non-rundown questions so agent remains broadly helpful.
- [x] Validate with sample prompts (e.g., "explore this", "plan this", migration authoring requests) and confirm behavior matches intent.
- [x] verify: build and test ok

Change the defaultl Scan count for planning to 3 min.

- [x] Locate where the planning scan-count default is defined and change the default value to `3`.
- [x] Update planning option parsing/config resolution so omitted scan-count uses the new default of `3`.
- [x] Add or update tests to verify planning runs `3` scans when no scan-count is explicitly provided.
- [x] Update user-facing docs/help text that mention the planning scan-count default to reflect `3`.

--revertable and --commit --commit-message and --commit-mode can be configured in config and change by default value

- [x] Add config support for `revertable`, `commit`, `commit-message`, and `commit-mode` options.
- [x] Update option resolution so CLI flags and config values are merged predictably for `--revertable`, `--commit`, `--commit-message`, and `--commit-mode`.
- [x] Change default behavior to use configured defaults for `--commit`, `--commit-message`, and `--commit-mode` when flags are not provided.
- [x] Add or update tests covering config-driven values and flag precedence for `--revertable`, `--commit`, `--commit-message`, and `--commit-mode`.

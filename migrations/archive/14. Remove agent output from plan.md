In plan command I see agent output by default. We don't output it now for run (without explicit attibute). Please check that plan works the same way please.

## Background

The current behavior appears inconsistent between `run` and `plan`:

- `run` no longer shows agent output by default (unless explicitly enabled via an attribute/flag).
- `plan` still appears to show agent output by default.

This migration is intended to align `plan` with the newer `run` behavior so both commands follow the same default visibility rules.

## Goal

Ensure `plan` does **not** emit/show agent output by default, matching `run` behavior when no explicit output-related attribute is provided.

## Current vs Desired Behavior

### Current
- `plan`: agent output is visible by default.
- `run`: agent output is hidden by default unless explicitly requested.

### Desired
- `plan`: agent output is hidden by default.
- `run`: unchanged (already hidden by default without explicit attribute).
- Explicit configuration/attribute should continue to override defaults for both commands.

## Functional Expectations

- Default invocation of `plan` should not include agent output sections in terminal/UI/log output intended for user-facing display.
- If an explicit attribute/option enables agent output, `plan` should still show it.
- Existing explicit disable behavior (if present) should continue to work.
- Output formatting should remain stable apart from removal of default agent output blocks.

## Technical Context to Verify

Identify where `plan` and `run` each determine output visibility and compare the decision path:

- Command argument parsing (CLI option/attribute resolution).
- Default configuration layer (global config, command-level defaults, runtime overrides).
- Rendering/presentation layer where agent output gets appended/emitted.
- Any shared utility used by `run` but not yet by `plan` (or vice versa).

The likely fix is to reuse the same defaulting logic path, or centralize a single helper so both commands derive `showAgentOutput` consistently.

## Constraints and Compatibility

- Do not change explicit user intent: explicit `show`/`hide` settings must continue to win over defaults.
- Avoid regressions in non-interactive/scripted usage where output shape may be parsed.
- Preserve backward compatibility except for the intended default behavior correction in `plan`.
- Keep behavior consistent across TTY and non-TTY environments unless existing architecture intentionally diverges.

## Edge Cases

- `plan` with no attributes/options.
- `plan` with explicit agent-output enabled.
- `plan` with explicit agent-output disabled.
- `plan` when config file sets a default (ensure precedence is correct).
- Mixed command contexts where `plan` triggers sub-agents internally but user-facing output should still obey visibility rules.

## Integration Notes

- If `run` already has mature logic for “default hidden unless explicit attribute,” prefer wiring `plan` into that same logic instead of duplicating conditionals.
- Confirm naming consistency around “attribute”/“option”/“flag” in code and docs to reduce future drift.
- If there are docs/help text describing `plan` output defaults, update them to match behavior.

## Acceptance Criteria Outline

- `plan` without explicit output attribute does not show agent output.
- `run` behavior remains unchanged.
- Explicit output attribute on `plan` continues to work.
- Tests (existing or added) cover parity between `run` and `plan` default output behavior.
- No unrelated output regressions in command rendering.

- [x] Trace `run` and `plan` code paths to locate where `showAgentOutput` (or equivalent) is resolved from CLI attributes/options, config, and runtime defaults.
- [x] Refactor or introduce a shared resolver for agent-output visibility defaults so `plan` and `run` use the same precedence logic.
- [x] Update `plan` default behavior to hide agent output when no explicit output-related attribute/option is provided.
- [x] Verify explicit enable/disable attributes for `plan` still override defaults and preserve current explicit-intent precedence rules.
- [x] Ensure rendering/output emission in `plan` skips agent output blocks by default without changing unrelated output formatting.
- [x] Validate parity across interactive (TTY) and non-interactive modes so default-hidden behavior for `plan` is consistent unless intentionally differentiated.
- [x] Add or update tests covering: `plan` default no-attribute case, explicit enable, explicit disable, config-provided default, and parity with `run`.
- [x] Add a regression test ensuring `run` behavior remains unchanged after shared-logic or defaulting changes.
- [x] Update CLI/help/docs text that describes `plan` agent-output defaults to match the new default-hidden behavior.
- [x] Add or update a test for `plan` flows that invoke sub-agents internally to ensure user-facing agent output remains hidden by default unless explicitly enabled.
- [x] Normalize terminology in code and docs for the output control (`attribute`/`option`/`flag`) to consistently reference the same agent-output visibility setting.

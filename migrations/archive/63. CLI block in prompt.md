Support fenced Markdown `cli` blocks in prompt files the same way they are already supported for target/source task documents.

Context: we already have ```cli behavior in task-processing flows, but prompt-file handling should apply the same parsing/execution semantics (or confirm it already does). If missing, implement parity; if already present, add coverage to lock behavior.

- [x] Verify current behavior for fenced `cli` blocks in prompt files and compare it to target/source document handling.
- [x] If behavior differs, implement prompt-file parsing/execution parity for ```cli blocks using the existing target-flow rules.
- [x] Ensure block semantics match existing behavior (ordering, output handling, and failure handling) rather than introducing a new variant.
- [x] Add/update tests covering prompt-file `cli` fence handling, including at least one passing and one failing command case.
- [x] Document the final behavior (implemented now or already existing) so prompt-file support is explicit and non-ambiguous.
- [x] verify: build and test ok

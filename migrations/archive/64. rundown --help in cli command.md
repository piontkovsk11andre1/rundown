Add a fenced `cli` block to the agent help template that runs:

```
rundown --help --everything
```

Purpose: expose the real, current CLI help output for all commands/options directly inside the help prompt context, so the agent can rely on the authoritative runtime help instead of stale manually maintained command lists.

- [x] Update the help template used for agent prompts to include a `cli` fence executing `rundown --help --everything`.
- [x] Ensure this block is executed in the same prompt-processing flow as other supported `cli` fenced blocks.
- [x] Confirm the resulting help output is available to the agent context (not stripped) and remains readable.
- [x] Verify behavior in a real help run and confirm no regressions in existing help-template rendering.
- [x] verify: build and test ok

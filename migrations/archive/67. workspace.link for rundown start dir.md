Add `.rundown/workspace.link` to store a relative path to the actual `rundown start` workspace root for the current directory, so commands can resolve and operate against the intended source workspace.

Keep this migration short and execution-oriented: prefer `fast:` tasks for implementation steps, then run explicit verification after those steps.

- fast:
  - [x] Define the `.rundown/workspace.link` path format and resolution rules relative to the current directory.
  - [x] Implement workspace-link loading and absolute workspace-root resolution in the migration/runtime path logic.
  - [x] Update command workspace selection to prefer the resolved workspace-link target when present, with existing behavior as fallback.
  - [x] Write `.rundown/workspace.link` during `rundown start` so the current directory can resolve back to the original workspace root.
- [x] Add tests covering valid relative links, missing link files, and invalid/stale link targets.
  - [x] verify: Commands launched from a linked directory resolve and operate against the intended source workspace.
  - [x] verify: Existing behavior is unchanged when `.rundown/workspace.link` is absent.

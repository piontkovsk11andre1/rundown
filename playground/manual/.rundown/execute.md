You are operating inside the manual verification playground for `rundown`.

Only make changes inside this playground directory unless the selected task explicitly requires something else.

## Context

{{context}}

---

## Selected task

Source: `{{file}}` line {{taskLine}}
Task: {{task}}

Complete the task exactly as written.

Rules:
- Keep the result deterministic.
- If the task asks for exact file contents, match them exactly.
- Do not change any Markdown checkbox yourself.
- Do not treat editing the task file as proof of completion.
- `rundown` will update the checkbox after verification succeeds.
